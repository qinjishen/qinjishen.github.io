---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { render } from "astro:content";
import { formatDate, getPostTranslation, getPostsByLang, languages, toPostUrl } from "../../../lib/blog";

export async function getStaticPaths() {
  const posts = await getPostsByLang("en");
  return posts.map((post) => ({
    params: { slug: post.data.permalink },
    props: { post }
  }));
}

const { post } = Astro.props;
const { Content } = await render(post);
const dictionary = languages.en;
const translation = await getPostTranslation(post);
---

<BaseLayout
  title={`${post.data.title} | Qin Ji Shen`}
  description={post.data.description}
  lang="en"
  currentPath={`/en/blog/${post.data.permalink}/`}
>
  <article class="article-shell">
    <header class="article-header">
      <div class="article-meta">
        <span>{dictionary.publishDate}</span>
        <time datetime={post.data.date.toISOString()}>{formatDate(post.data.date, "en")}</time>
        {post.data.updatedDate && (
          <span>{dictionary.updatedDate} {formatDate(post.data.updatedDate, "en")}</span>
        )}
      </div>
      <h1>{post.data.title}</h1>
      <p class="article-description">{post.data.description}</p>
      {translation && <a class="translation-link" href={toPostUrl(translation)}>阅读中文版 →</a>}
    </header>
    <div class="article-body"><Content /></div>
  </article>
</BaseLayout>
